fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.1
numpy>=1.26
python-dateutil>=2.9

# 可选增强（不装也能跑，代码会自动降级）：
# trafilatura          正文提取质量更高
# openai               接入 OpenAI 兼容 LLM / embedding
# sentence-transformers 本地 embedding（如 BGE/E5）
